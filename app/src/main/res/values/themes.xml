<resources>
    <!-- Base application theme. -->
    <style name="Theme.Listings" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/grayedOutTextColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NoAppBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!--      TODO CHECK THESE FLAGS  -->
        <!--        <item name="windowActionBar">false</item>-->
        <!--        <item name="windowActionBarOverlay">false</item>-->
        <!--        <item name="android:windowIsFloating">false</item>-->
        <!--        <item name="android:windowActionModeOverlay">false</item>-->
        <!--        <item name="android:windowNoTitle">true</item>-->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
        <item name="android:radioButtonStyle">@style/AppRadioButton</item>
        <item name="popupTheme">@style/BasePopUpMenu</item>
        <item name="popupMenuStyle">@style/BasePopUpMenu</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:buttonBarPositiveButtonStyle">@style/AlertDialogPositiveButton</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/AlertDialogNegativeButton</item>
    </style>

    <style name="AlertDialogPositiveButton">
        <item name="background">@null</item>
        <item name="android:textColor">@color/accentPurple</item>
    </style>

    <style name="AlertDialogNegativeButton">
        <item name="background">@null</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:textColor">@color/highlightRed</item>
    </style>

</resources>